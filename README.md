# capstone
FPGA-based fixed-point CNN inference accelerator designed in Verilog-2002. The project implements a modular RTL datapath including convolution, ReLU, pooling, dense layers, and threshold-based binary classification, with layer-wise verification against Python golden references.
